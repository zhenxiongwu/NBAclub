<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus1.nbatest">

    <application
        android:name=".application.NBAApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_appicon1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".activities.LaunchActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.HomeActivity"
            android:configChanges="orientation|keyboard|screenSize">
        </activity>
        <activity
            android:name=".activities.PlayerDetailActivity"
            android:label="" />
        <activity android:name=".activities.StatisticsActivity" />
        <activity android:name=".activities.TeamDetailActivity"
            android:label=""/>
    </application>

</manifest>